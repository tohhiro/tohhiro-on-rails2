<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
  <li>
    <h2><%= post.title %></h2>
    <p><%= post.content.present? ? post.content : "なし" %></p>
    <span><%= post.created_at.strftime("%Y-%m-%d %H:%M") %></span>
  </li>
  <% end %>
</ul>
